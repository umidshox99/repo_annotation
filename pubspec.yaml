name: repo_annotation
description: A new Flutter project.
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.0.0 <3.10.3'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

